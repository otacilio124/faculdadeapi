# Neon Postgres — pooler (queries em runtime)
# Formato: postgresql://<user>:<senha>@<host>-pooler.<regiao>.aws.neon.tech/<db>?sslmode=require
DATABASE_URL=""

# Neon Postgres — direto (Prisma Migrate)
# Mesmo host sem "-pooler"
DIRECT_URL=""

# Segredo JWT — gere com: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=""
